# Copier en .env.local pour le développement local.

# URL de apps/api. Les formulaires de connexion/inscription n'appellent que
# cette URL en clair depuis le navigateur — l'API n'étant pas déployée
# (choix d'hébergeur en attente, plan de développement §9.5), ça ne
# fonctionne qu'en local pour l'instant, pas depuis la preview Vercel.
NEXT_PUBLIC_API_URL="http://localhost:3001"
